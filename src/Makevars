CXX_STD = CXX17

RCPP_PARALLEL_LIB = $(shell "${R_HOME}/bin/Rscript" -e \
    'cat(file.path(find.package("RcppParallel"), "libs"))')

PKG_LIBS += -L$(RCPP_PARALLEL_LIB) \
            -Wl,-rpath,$(RCPP_PARALLEL_LIB) \
            -lRcppParallel
